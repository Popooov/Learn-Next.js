/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ["9ppzj9-3000.csb.app"],
        }
    },
};

module.exports = nextConfig;
