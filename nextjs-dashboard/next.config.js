/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ["xvcgk2-3000.csb.app"],
        }
    },
};

module.exports = nextConfig;
