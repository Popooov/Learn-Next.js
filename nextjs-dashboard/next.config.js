/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ["hjd5qd-3000.csb.app"],
        }
    },
};

module.exports = nextConfig;
