/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: {
            allowedOrigins: ["xgg93q-3000.csb.app"],
        }
    },
};

module.exports = nextConfig;
